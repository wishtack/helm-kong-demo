apiVersion: v1
kind: Service
metadata:
  name: {{ include "kong-demo.fullname" . }}
  labels:
{{ include "kong-demo.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "kong-demo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
